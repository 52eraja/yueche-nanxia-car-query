# v1.0 重构说明

## 变更

- **更名**：粤车南下车牌查询 v2.4 → 粤车南下来往港澳车辆备案信息查询 v1.0
- **skill name**：`yueche-nanxia-car-query` → `yueche-nanxia-vehicle-registry`
- **版本重置**：2.4.0 → 1.0.0（重大重构，版本号重置）

## 核心简化

用户明确方向：**聚焦香港车牌查询，确保稳定可靠**。

- 香港车牌 → Python urllib（快速，有重试）
- 内地车牌 → Playwright（需要时再用）
- 不再花时间测试内地车牌的 urllib 可行性

## 经验教训

1. **szcport 服务器间歇性断连**是常态，不是异常。重试机制是必需的。
2. **urllib 查内地车牌始终返回无数据**，这是接口限制，不是 bug。
3. **用户偏好直接操作**：给车牌号 → 拿结果，不要啰嗦。
4. **路径问题**：本环境技能在 `./skills/` 下，不在 `~/.hermes/skills/` 下。
